#include <stdio.h>
#include <stdlib.h>
#include <time.h>
/**
 * main - print a snigle digit between one and ten
 * Return: Always 0
 */
int main(void)
{
	int numbers; {

	for (numbers = 0; numbers < 10; numbers++);
	printf(numbers);

	printf("\n");

	return (0);
	}
}
