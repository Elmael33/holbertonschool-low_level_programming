#include <stdio.h>
#include <stdlib.h>
#include <time.h>
/**
 * main - print reverse alphabet in lower
 * Return: Always 0
 */
int main(void)
{
	char(reverse);

	for (reverse = 'z'; reverse >= 'a'; reverse--)
	{
		putchar(reverse);
	}

	putchar('\n');

	return (0);
}
