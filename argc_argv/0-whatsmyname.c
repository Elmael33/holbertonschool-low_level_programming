#include <stdio.h>
/**
 * main - prints its name
 * @argc: integer
 * @argv: pointer
 * Return: (always) succes
 */
int main(int argc __attribute__((unused)), char *argv[])
{
	{
		printf("%s\n", argv[0]);
	}
	return (0);
}
