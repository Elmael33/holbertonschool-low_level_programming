#include "main.h"
#include <unistd.h>
#include <islower>
/**
 * _islower - checks for lowercase character
 * Return: Always 0
 */
int _islower(int c);
{
	char check;

	if (check = 'a'; check <= 'z'; check++)


	else if (check = 'A'; check <= 'Z'; check++)


	else (check !='1'; check <= '0')

	return (0);

}
