#include "main.h"
#include <unistd.h>
/**
 * add - adds two integers and returns the result
 * @oui: integers
 * @non: integers
 * Return: ca_va
 */
int add(int oui, int non)
{
	int ca_va;

	ca_va = oui + non;

	return (ca_va);
}
