#include "main.h"
#include <unistd.h>
/**
 * print_sign - print the sign of a number
 * Return: Always 0
 */
int print_sign(int n)
{
	char numo;

	if (numo > '0')
	{
		printf(+)
		return (1);
	}

	else if (numo < '0')
	{
		printf(-)
		return (-1);
	}
	else 
	{
		printf(0)
		return (0);
	}
}

